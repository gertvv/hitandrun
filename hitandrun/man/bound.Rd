\name{bound}
\alias{createSeedPoint}
\alias{createBoundBox}
\title{
Create seed points and bounding boxes.
}
\description{
%%  ~~ A concise (1-5 lines) description of what the function does. ~~
Utilities to generate a seed point or bounding box from a constraint matrix.
}
\usage{
createSeedPoint(constr, homogeneous=FALSE)
createBoundBox(constr, homogeneous=FALSE)
}
\arguments{
	\item{constr}{Constraint definition}
	\item{homogeneous}{Whether constr is given in homogeneous coordinate representation}
}
\details{
See \code{\link{har}} for a description of the constraint definition and the homogeneous coordinate representation.

\code{createSeedPoint} and \code{createBoundBox} solve 2n linear programs (using \code{lpSolve}) to find extreme points along each of the n dimensions.
If a homogeneous coordinate is present, a constraint is added that the homogeneous coordinate must equal 1.0.
The seed point is generated by taking the mean of the extreme points.
}
\value{
For createSeedPoint: a coordinate vector in the appropriate coordinate system.

For createBoundBox:
\item{lb}{Lower bound for each dimension (not including homogeneous coordinate).}
\item{ub}{Upper bound for each dimension (not including homogeneous coordinate).}
\item{extreme}{The extreme points themselves.}
}
\references{
%% ~put references to the literature/web site here ~
}
\author{
Gert van Valkenhoef
}
\note{
%%  ~~further notes~~
}

%% ~Make other sections like Warning with \section{Warning }{....} ~

\seealso{
\code{\link{har}}
}
\examples{
# constraints: x_1 >= 0, x_2 >= 0, x_1 + x_2 <= 1
A <- rbind(c(-1, 0), c(0, -1), c(1, 1))
b <- c(0, 0, 1)
d <- c("<=", "<=", "<=")
constr <- list(constr=A, rhs=b, dir=d)

x0 <- createSeedPoint(constr)
stopifnot(x0 >= 0)
stopifnot(sum(x0) <= 1)

bb <- createBoundBox(constr)
stopifnot(bb$lb == c(0.0, 0.0))
stopifnot(bb$ub == c(1.0, 1.0))
}
% Add one or more standard keywords, see file 'KEYWORDS' in the
% R documentation directory.
\keyword{seed point}
\keyword{bounding box}
